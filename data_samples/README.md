# Data Samples

This directory contains sample CSV files for testing the detection engine.

## Format

All CSV files must contain these columns:

- `transaction_id` — Unique transaction identifier
- `sender_id` — Account sending funds
- `receiver_id` — Account receiving funds
- `amount` — Transaction amount (numeric)
- `timestamp` — In format `YYYY-MM-DD HH:MM:SS`
