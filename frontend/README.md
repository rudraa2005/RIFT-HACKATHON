# Frontend

Place frontend files here (HTML, CSS, JS, React/Next.js, etc.).

The backend API runs at `http://localhost:8000` and provides:
- `POST /upload` — Upload CSV, returns detection results JSON
- `GET /health` — Health check
- `GET /metrics` — Processing statistics
